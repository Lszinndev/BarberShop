# Copy to .env and set if you want the frontend to call the API directly
# Leave empty or unset to use the CRA proxy defined in package.json (development)
# Example for local backend running on port 5105:
REACT_APP_API_URL=http://localhost:5105
